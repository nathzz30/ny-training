// 'use strict';

// const sanitize = require('../../services/universal/sanitize');

// module.exports.save = function(ref, data) {
//   data.descriptionRecipe = sanitize.validateTagContent(sanitize.toSmartText(data.descriptionRecipe || ''));
//   return data;
// };
